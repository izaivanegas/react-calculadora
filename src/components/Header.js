import React from "react";
import Logo from "../asserts/logo.jpg"

//export default function Header(){
const miFunction = ()=>{
    return(
        <div>
            <img   src={Logo} className="logo"  alt="Esto es una imagen" />

        </div>
    );
}

export default miFunction;